# TASK-UL037: Session-hygiene commits lack task traceability

## Context
Filed retroactively (E-9 traceability nudge) after commit 0662215 ("chore: Session hygiene —
commit stream and session-state updates") landed without a TASK-UL reference. Routine
housekeeping commits of CONSCIOUSNESS/stream/*.jsonl and .claude/ session-state files are a
byproduct of every substantive task worked in a session, not standalone work — but they still
need a traceable ID per the commit-task traceability target (E-9 ≥ 0.9).

## Acceptance Criteria
- [x] This card exists so commit 0662215 has a retroactive task reference
- [x] Adopt the practice going forward: reference the most-recently-completed or currently
      in-progress TASK-UL ID directly in session-hygiene commit messages (the stream files
      being committed are telemetry generated by that task's work), rather than filing a new
      retroactive card per hygiene commit

## Technical Notes
No code changes — process/traceability fix only. Future hygiene commits in this session
reference TASK-UL23 (or whichever task is active) directly instead of going untracked.

## Dependencies
- Blocked by: none
- Blocks: none
